# ML016 Atamisquea recovery checkpoint

The structural source-frame gate has passed, but ML016 is **not yet admitted**.

Remaining pre-admission checks:

1. verify the site-level Hedges-g variance calculation against the repository's canonical `metafor::escalc(measure="SMD", vtype="LS")` semantics;
2. construct cross-layer I/F sampling dependence from the four matched landscape sites using the same rule already accepted for primary clusters;
3. run the unchanged two-layer state-separation test;
4. only then recompute the five-cluster and omit-ML001 Fisher diagnostics.

No endpoint/species/year substitution is permitted after the contract lock.
