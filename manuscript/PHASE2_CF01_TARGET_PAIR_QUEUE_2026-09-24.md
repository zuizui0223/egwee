# Phase-2 CF01 target-pair candidate queue — 2026-09-24

## Result

The outcome-blind abstract triage contains **1120** priority multilayer-fragmentation candidates.

Of those, **360** carry metadata for at least one already-registered pair family:

- I-F memberships: **274**;
- C-F memberships: **166**;
- G_adult-mean(I,F) memberships: **61**.

Membership counts overlap when one publication carries multiple pair hints.

## Workload ordering

The queue order is fixed without outcomes:

1. DOI + named venue + abstract available;
2. remaining target-pair candidates;
3. within each tier, publication year descending and then OpenAlex ID.

This is only a screening order. It is not an inclusion rule. Every target-pair candidate remains in the search denominator until a documented screen decision is assigned.

No effect direction, magnitude, variance, p-value or significance field was opened to construct this queue.

## Next operation

Screen the first deterministic wave using the frozen CF01 candidate-screening contract. Abstract ambiguity advances to full-text clarification rather than exclusion.
