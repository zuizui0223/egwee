name: Penstemon 2019 schema gate

on:
  pull_request:
    paths:
      - 'manuscript/PENSTEMON_2019_ISOLATION_CLUSTER_RECOVERY_CONTRACT.md'
      - 'manuscript/PENSTEMON_2019_ISOLATION_CLUSTER_RECOVERY_RESULT.md'
      - 'evidence/meta_extraction/C07_penstemon_2019_schema_gate_v1.csv'
      - 'evidence/meta_extraction/multilayer_cluster_registry_v1.csv'
      - 'scripts/probe_penstemon_2019_zenodo_schema.py'
      - 'scripts/check_penstemon_2019_recovery_contract.py'
      - '.github/workflows/penstemon-2019-schema.yml'
  workflow_dispatch:

jobs:
  schema:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Inspect Zenodo metadata and CSV schema only
        run: python scripts/probe_penstemon_2019_zenodo_schema.py
      - name: Enforce terminal multilayer recovery gate
        run: python scripts/check_penstemon_2019_recovery_contract.py
